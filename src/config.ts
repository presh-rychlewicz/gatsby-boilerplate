import { ConfigContext } from "./contexts"

export const config: ConfigContext = {
  pageTitles: {
    index: "Home Page",
    404: "Not found",
  },

  translations: {
    downloadButton: "Download",
  },
}
